---
import Container from "../Container.astro";
import Button from "../Button.astro";
import { KAKAO } from "../../../lib/links";
---

<header
  id="site-nav"
  class="sticky top-0 z-40 border-b border-border bg-page/80 backdrop-blur-md"
>
  <Container class="flex h-full items-center justify-between">
    <a href="/" class="flex items-center gap-2.5">
      <span
        data-nav-logo
        class="flex size-8 items-center justify-center rounded-lg bg-primary text-on-primary text-sm font-bold"
        >L</span
      >
      <span class="text-base font-semibold tracking-tight">LeadGenLab</span>
    </a>
    <nav class="hidden items-center gap-8 text-sm text-fg-muted md:flex">
      <a href="/#solution" class="transition-colors hover:text-fg">솔루션</a>
      <a href="/#framework" class="transition-colors hover:text-fg">프레임워크</a>
      <a href="/#metrics" class="transition-colors hover:text-fg">성과</a>
      <a href="/glossary/" class="transition-colors hover:text-fg">용어집</a>
      <a href="/#faq" class="transition-colors hover:text-fg">FAQ</a>
    </nav>
    <Button href={KAKAO} size="md" class="hidden sm:inline-flex">무료 진단 요청</Button>
  </Container>
</header>

<style>
  /* 스크롤 시 줄어드는 상단바 — 높이는 헤더 자체에(Navbar 소유 → 스코프 정확). 링크는 정적 HTML. */
  #site-nav {
    height: 4rem;
    transition:
      height 0.3s ease,
      background-color 0.3s ease,
      box-shadow 0.3s ease;
  }
  #site-nav.is-scrolled {
    height: 3rem;
    box-shadow: var(--shadow-card);
    background-color: color-mix(in oklab, var(--color-page) 92%, transparent);
  }
  [data-nav-logo] {
    transition:
      width 0.3s ease,
      height 0.3s ease;
  }
  #site-nav.is-scrolled [data-nav-logo] {
    width: 1.6rem;
    height: 1.6rem;
    border-radius: 0.4rem;
  }
</style>

<script>
  const nav = document.getElementById("site-nav");
  const onScroll = () => nav?.classList.toggle("is-scrolled", window.scrollY > 12);
  onScroll();
  addEventListener("scroll", onScroll, { passive: true });
  // View Transitions 후에도 동작하도록 재바인딩
  document.addEventListener("astro:page-load", () => {
    const n = document.getElementById("site-nav");
    n?.classList.toggle("is-scrolled", window.scrollY > 12);
  });
</script>
